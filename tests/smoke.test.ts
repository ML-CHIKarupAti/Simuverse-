import { describe, it, expect } from 'vitest'

// Placeholder smoke test to confirm the vitest harness is wired.
// Real logic tests arrive with step 0.3 (schema round-trip) onward.
describe('vitest harness', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2)
  })
})
